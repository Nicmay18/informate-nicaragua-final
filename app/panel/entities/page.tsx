import type { Metadata } from 'next';
import EntitiesPageContent from '@/components/nios/EntitiesPageContent';

export const metadata: Metadata = {
  title: { absolute: 'Entidades | NIOS' },
};

export default EntitiesPageContent;
