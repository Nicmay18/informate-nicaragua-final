import type { Metadata } from 'next';
import NiosPanelPageContent from '@/components/nios/NiosPanelPageContent';

export const metadata: Metadata = {
  title: { absolute: 'NIOS | Panel' },
};

export default NiosPanelPageContent;
