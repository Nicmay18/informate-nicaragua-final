{
  "headers": [
    {
      "source": "/feed.xml",
      "headers": [{ "key": "Content-Type", "value": "application/xml; charset=utf-8" }]
    }
  ],
  "buildCommand": "rm -rf .next node_modules package-lock.json && npm install && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "github": {
    "silent": true
  }
}
